const Footer = ({ children }) => {
  return (
    <footer className="w-screen h-[15vh] bg-black text-white flex justify-center items-center mt-8">
      {children}
    </footer>
  );
};

export default Footer;
